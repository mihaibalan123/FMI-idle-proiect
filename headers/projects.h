#ifndef OOP_PROJECTS_H
#define OOP_PROJECTS_H

/*
class projects {
    std::string name;
    float price, cashback;
    int number;
public:
    projects(std::string name, float price, float cashback, int number): name(name), price(price), cashback(cashback){};
    ~projects()=default;
};
*/

#endif //OOP_PROJECTS_H